# laravel_discussion_board

## Run Container

```
docker-compose up -d nginx mysql phpmyadmin redis workspace
```

## Enter the Workspace Container

```
docker-compose exec --user=laradock workspace bash
```
